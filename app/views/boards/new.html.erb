<%= content_for :title, t('boards.new.title') %>
<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1><%= t('boards.new.title') %></h1>
      <%= render partial: 'boards/form' %>
    </div>
  </div>
</div>