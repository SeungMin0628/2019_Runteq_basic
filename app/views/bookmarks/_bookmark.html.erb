<%= 
  link_to(
    content_tag(:i, '', class: 'fas fa-star'),
    board_bookmarks_path(board_id: board.id),
    method: :post,
    remote: true,
    id: "btn-bookmark-#{board.id}",
    rel: 'nofollow'
  ) 
%>