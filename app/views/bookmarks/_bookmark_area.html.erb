<% if (bookmark = current_user.bookmark_for board) %>
  <%# ブックマークされている場合、　解除ボタンを付ける %>
  <%= render 'bookmarks/unbookmark', board: board, bookmark: bookmark %>
<% else %>
  <%# ブックマークされていない場合、　ブックマークボタンを付ける %>
  <%= render 'bookmarks/bookmark', board: board %>
<% end %>
